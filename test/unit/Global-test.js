suite('Global', function() {

    // ======================================================
    test('test Kinetic version number', function() {
        assert.equal(Kinetic.version, 'dev');
    });
});